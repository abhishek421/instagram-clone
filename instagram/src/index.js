import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';
import reportWebVitals from './reportWebVitals';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

// If you want to start measuring performance in your app, pass a function // components props  -- in 70 <pp /> pp("url")
// to log results (for example: reportWebVitals(console.log)) html in js // html-structure,css-design,js-fuction // js
// or send to an analytics endpoint. Learn more: https://bit.ly/CRA-vitals // JS -> CSS,HTML // CSS // import
reportWebVitals();
